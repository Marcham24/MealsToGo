import { Text } from "react-native";
import { SafeView } from "../../../components/saveview";

export const MapScreen = () => {
  return (
    <SafeView>
      <Text>Hello Map</Text>
    </SafeView>
  );
};
