import { Text } from "react-native";
import { SafeView } from "../../../components/safeview";

export const MapScreen = () => {
  return (
    <SafeView>
      <Text>Hello Map</Text>
    </SafeView>
  );
};
